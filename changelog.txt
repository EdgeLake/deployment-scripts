* Blob storage:
1. set_params.al - updated param names + added comments (L 154-193)
-- blob-storage
-- blob-dbms
-- blob-bucket

To look into - what happens if someone wants this:
-> local MongoDB or MinIO
-> remote S3 or Akave storage


2. New scripts
-- configure_blob_storage.al - select storage + configure archiver
-- connect_dbms_nosql.al - NoSQL storage (ex. MongoDB)
-- connect_dbms_objstore.al - Akave code

3. updated file names / paths in database/ for consistency
-- simplify connection process to 1 file rather than "unique" per database
-- updated deploy_database.al with correct paths

* Node Types - we should now support:
-- master
-- operator
-- query
-- publisher
-- master-operator (This is an operator node with blockchain.ledger)
-- master-publisher (this is a publisher node with blockchain.ledger)

Note: `system_query` can still be deployed by simplify setting `SYSTEM_QUERY=true`
